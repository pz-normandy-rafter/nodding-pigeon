[build-system]
requires = [
    "setuptools>=42",
    "wheel"
]

[tool.poetry.dependencies]
python = ">=3.9"
opencv-contrib-python = "^4.12.0"
gdown = "5.2.0"
mediapipe-numpy2 = "^0.10.21"
tensorflow = ">=2.7.0"
